VITE_API_BASE_URL=http://localhost:8081

# When deploying to Vercel, set VITE_API_BASE_URL to your backend URL, e.g.:
# VITE_API_BASE_URL=https://your-backend.onrender.com

# Note: Vite-based environment variables that should be exposed to the client must be prefixed with VITE_.
